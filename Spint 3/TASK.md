Frontend Profile Screen Integration
Connect React Native profile screen to the backend API, replacing all hardcoded data with real Firestore reads and live state updates.
Firebase Storage Photo Management
Implement photo upload, gallery display, and delete functionality using Firebase Storage, with photos saved as URL arrays in user profiles.
Edit & Delete Profile
Wire up the edit profile modal and delete button to the backend API, supporting live updates to name, bio, location, interests, skill level, attitude and Instagram handle.

